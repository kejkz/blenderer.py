image: atlassian/default-image:latest
pipelines:
  default:
    - step:
        caches:
          - pip
        script:
          - apt-get update && apt-get install -y blender libav-tools ffmpeg
          - ./example.sh
